services:
  postgres:
    image: postgres:latest
    container_name: crm-postgres
    environment:
      POSTGRES_DB: shift-lab-crm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    ports:
      - "5433:5432"
